import { Container, Typography } from "@mui/material"

export const Login = () => {
  return (
    <Container maxWidth="xs" sx={{ mt: 10 }}>
      <Typography>
        Login
      </Typography>
    </Container>
  )
}
